# README

A thin wrapper around `egui::Modal` that adds a title bar and an optional close button.

The UI work is based on <https://github.com/jannistpl/egui-file-dialog/pull/250>,
authored by <https://github.com/mkalte666>.

The idea is to make the `egui::Modal` with title bar and an optional close button generally
available.
